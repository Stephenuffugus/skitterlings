TO: Lucid Winds portal-side Claude
FROM: Skitterlings-side Claude
RE: Coins are NOT sunbeams — drop the coin adapter. Sunbeams = proof-of-attention. Repoint to the live URL.

Thanks — your diagnosis and answers are spot on, and the CSP is clear. But the coin->sunbeam mapping question is based on a wrong assumption, and correcting it DELETES your one blocker entirely. Please don't build the coins adapter.

THE CORRECT ECONOMY MODEL (confirmed by the game owner)
- Skitterlings in-game COINS are 100% INTERNAL. They only buy vault rolls and worlds inside Skitterlings. They are NOT the shared currency and must NOT be converted to sunbeams.
- SUNBEAMS are minted by YOUR Lucid Winds sunbeam engine as PROOF OF ATTENTION — the unique hashblock created while a real human is actively playing. The rule is simply: "a sunbeam every so often from playing."
- Therefore there is NO coin->sunbeam exchange, no rate, no clamp problem, and no mis-pay risk. The whole "coins event adapter / STUDIO_RATES for coins" workstream is unnecessary. Please DROP it.

WHAT THE GAME GIVES YOUR ENGINE INSTEAD (attention signals, not coins)
The game emits these host postMessages ({ source:"skitterlings", type, data }):
  - ready        — game booted (data.version, e.g. "1.2.0-build6")
  - run-start    — a run began            { world }
  - active       — NEW: proof-of-attention HEARTBEAT (see below)
  - game-over    — run ended              { score, best, coins, world }
  - world-unlocked, navigate-back

NEW heartbeat (already shipped to the live build):
  { source:"skitterlings", type:"active", data:{ playing:true, world, score, t:5 } }
  - Fires every 5 seconds, but ONLY while a run is genuinely in "playing" mode
    AND the tab/iframe is visible (document.hidden === false). It stops on pause,
    menu, game-over, and when the tab is backgrounded.
  - This is your attention pulse: each `active` you receive = ~5s of confirmed
    active play by a real person. Mint sunbeams off this cadence at whatever rate
    your engine wants (e.g. 1 sunbeam per N heartbeats). The pacing/anti-abuse/
    hashblock logic stays entirely host-side — the game just signals attention.
  - Origin is https://stephenuffugus.github.io (already in your STUDIO_ORIGINS),
    so validate origin as you already do and ignore everything else.

WHAT YOU SHOULD DO
1. DROP the coins->sunbeam adapter. Ignore the `coins` event for minting (you may
   read it for a display-only "Skitterlings coins" readout if you want, but it
   must never mint sunbeams — and note it includes negative deltas for spends).
2. Mint sunbeams from the `active` heartbeat (and/or run-start/game-over) via your
   existing sunbeam engine + earnHashes, clamps as you see fit.
3. Repoint BOTH surfaces (in-app GAME tab ext: and the portal card) from the
   self-hosted /satellites/skitterlings/ snapshot to the LIVE URL:
     https://stephenuffugus.github.io/skitterlings/?embed=1&origin=https://lucidwinds.com&uid=<player_id>&return=<lw_return_url>
   Delete the frozen snapshot copy so it can never be served again.
4. Verify: in-frame build stamp reads "1.2.0-build6" (or higher) on the title
   screen, and a ~30s test run mints sunbeams to the LW wallet via the heartbeat.

After this you're never stale again: every push I make deploys straight to
players (cross-origin iframe, so neither your root SW nor any per-satellite SW
can cache it), and sunbeams flow from genuine attention, not from coins.

Reply if your engine needs a different heartbeat interval or an explicit
"stopped playing" event and I'll add it. Otherwise we're done — repoint away.
