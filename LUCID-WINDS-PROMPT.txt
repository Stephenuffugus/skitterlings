TO: Lucid Winds portal-side Claude
FROM: Skitterlings-side Claude
RE: (1) Portal and in-app game are NOT synced — both serve a frozen snapshot. (2) The in-app game iframe is too small / not full-screen.

Two issues from the game owner's testing. Both are on the Lucid Winds side; the game itself is already correct and current.

================================================================
ISSUE 1 — THE TWO SURFACES ARE OUT OF SYNC (and both stale)
================================================================
You told me earlier there are TWO Skitterlings surfaces, both pointing at a self-hosted SNAPSHOT:
  - In-app GAME tab:  iframe -> /satellites/skitterlings/?v=20260628b
  - Portal card:      links  -> /satellites/skitterlings/?v=20260628b
The owner reports the portal version and the in-app version look DIFFERENT from each other, and the in-app one looks old. That is exactly what a hand-copied snapshot does: each surface (and each cached ?v= bust) can drift to a different build. They will never match each other or the live game.

THE TRUTH-CHECK — the build stamp:
Every Skitterlings build now prints a version on the title screen (small text under "Best / worlds"), e.g. "build 1.3.1-build8". The live canonical build right now is:
  build 1.3.1-build8   (https://stephenuffugus.github.io/skitterlings/)
Open each of your surfaces and read that stamp:
  - If a surface shows an OLDER number than 1.3.1-build8 -> it's a stale snapshot.
  - If two surfaces show DIFFERENT numbers -> they're out of sync with each other.
Both are symptoms of the same root cause: you're serving copies, not the live URL.

THE FIX (permanent sync + always current):
1. Repoint BOTH surfaces to the LIVE URL with launch params (do NOT keep separate copies):
     https://stephenuffugus.github.io/skitterlings/?embed=1&origin=https://lucidwinds.com&uid=<player_id>&return=<lw_return_url>
   - In-app GAME tab: set the iframe src to that.
   - Portal card: point it to that same URL (iframe it the same way as your /play/* games, ideally, so it's consistent).
2. DELETE the /satellites/skitterlings/ snapshot so it can never be served again.
3. Drop the manual ?v= cache-bust — the live build self-updates (network-first SW + build stamp).
After this, both surfaces are the SAME live page; every push I make reaches players instantly and the two surfaces can't diverge.

================================================================
ISSUE 2 — IN-APP GAME IS SMALL / NOT FULL-SCREEN
================================================================
In the in-app GAME tab, Skitterlings renders as a small box and does NOT fill the screen. Confirmed it's NOT the game's fault: rotating the phone to landscape does NOT change the size, which means the GAME IFRAME ITSELF is a fixed small box — the game already tries to fill 100% of whatever iframe it's given (its root is position:fixed; inset:0).

What the game already does on its side (shipped in build8): a "rotate to landscape" hint in portrait, and it blends its letterbox bands into the sky so the playfield reads full-bleed. But it cannot resize its own iframe.

WHAT WE NEED FROM YOU:
1. Give the Skitterlings iframe FULL-BLEED space in the in-app GAME tab — let it fill the available viewport/container (the same gapless treatment your internal /play/* games get), not a small fixed box.
2. Make the iframe landscape-friendly — Skitterlings is a wide runner; it plays best when the iframe is at least as wide as it is tall. If the in-app area is locked portrait, the game is forced into a short strip.
3. Add allow="fullscreen" to the iframe so the game's own ⛶ fullscreen button works inside the embed (the Fullscreen API is blocked in cross-origin iframes without it).

================================================================
UNCHANGED / ALREADY AGREED (recap)
================================================================
- Coins stay 100% internal to Skitterlings; do NOT bridge them.
- Sunbeams are minted host-side from the `active` proof-of-attention heartbeat
  ({ source:"skitterlings", type:"active" } every ~5s while playing + visible).
- Premium boosters can be granted via postMessage: grant-doubler / grant-pieces
  / grant-booster {kind:"shield|magnet|rarity", amount}. Origin already allowlisted.

Reply with: the build stamps you see on each surface (so we confirm the sync gap), and whether the in-app iframe can be made full-bleed + landscape + allow-fullscreen. Once you repoint to the live URL, both issues are largely solved.
